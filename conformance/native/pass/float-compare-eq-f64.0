pub fn main Void world World !
  let a f64 1.5
  let b f64 1.5
  let c f64 2.5
  let same Bool (== a b)
  let diff Bool (== a c)
  if && same (== diff false)
    check world.out.write "float eq f64 ok\n"
