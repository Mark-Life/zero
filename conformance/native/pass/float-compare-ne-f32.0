pub fun main(world: World) -> Void raises {
    let a: f32 = 1.5
    let b: f32 = 1.5
    let c: f32 = 2.5
    let sameNe: Bool = a != b
    let diffNe: Bool = a != c
    if sameNe == false && diffNe {
        check world.out.write("float ne f32 ok\n")
    }
}
