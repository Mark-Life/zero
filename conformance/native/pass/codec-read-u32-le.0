pub fun main(world: World) -> Void raises {
    let buf: [4]u8 = [255, 255, 255, 255]
    let span: Span<u8> = buf
    let read: u32 = std.codec.readU32Le(span, 0)
    let expected: u32 = 4294967295
    if read == expected {
        check world.out.write("codec read u32 le ok\n")
    }
}
