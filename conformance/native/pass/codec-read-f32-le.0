pub fun main(world: World) -> Void raises {
    let mut buf: [4]u8 = [0, 0, 128, 63]
    let span: Span<u8> = buf
    let read: f32 = std.codec.readF32Le(span, 0)
    let expected: f32 = 1.0
    if read == expected {
        check world.out.write("codec read f32 le ok\n")
    }
}
