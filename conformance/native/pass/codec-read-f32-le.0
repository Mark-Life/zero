pub fn main Void world World !
  mut buf [4]u8 [0, 0, 128, 63]
  let read f32 (std.codec.readF32Le buf 0)
  let expected f32 1.0
  if == read expected
    check world.out.write "codec read f32 le ok\n"
