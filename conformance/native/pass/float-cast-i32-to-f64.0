pub fun main(world: World) -> Void raises {
    let whole: i32 = 1000
    let asFloat: f64 = whole as f64
    if asFloat == 1000.0 {
        check world.out.write("float cast i32 to f64 ok\n")
    }
}
