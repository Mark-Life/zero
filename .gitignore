.DS_Store
dist/
.zero/
node_modules/
npm-debug.log*

# environment variables
.env*
!.env.example

# Large sparse fixture for the >4 GiB mmap-length round-trip test; generate locally
# (see .docs/llama2/phase-0-followups.md issue 3). Build-tolerated in CI, so never committed.
conformance/fixtures/mmap-4gib-sparse.bin

# llama2 example weights, downloaded per examples/llama2/README.md (60 MB checkpoint).
/stories15M.bin
/tokenizer.bin
